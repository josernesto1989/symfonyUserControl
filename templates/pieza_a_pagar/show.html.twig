{% extends 'base.html.twig' %}

{% block title %}PiezaAPagar{% endblock %}

{% block body %}
    <h1>PiezaAPagar</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pieza_a_pagar.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ pieza_a_pagar.nombre }}</td>
            </tr>
            <tr>
                <th>Precio</th>
                <td>{{ pieza_a_pagar.precio }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ pieza_a_pagar.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ventasmodule') }}">Módulo de ventas</a>

    <a href="{{ path('pieza_a_pagar_edit', {'id': pieza_a_pagar.id}) }}">edit</a>

    {{ include('pieza_a_pagar/_delete_form.html.twig') }}
{% endblock %}
