<form method="post" action="{{ path('tipo_pieza_delete', {'id': tipo_pieza.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_pieza.id) }}">
    <button class="btn">Delete</button>
</form>
