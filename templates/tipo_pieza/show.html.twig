{% extends 'base.html.twig' %}

{% block title %}TipoPieza{% endblock %}

{% block body %}
    <h1>TipoPieza</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_pieza.id }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ tipo_pieza.tipo }}</td>
            </tr>
            <tr>
                <th>ManoDeObra</th>
                <td>{{ tipo_pieza.manoDeObra }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tipo_pieza_index') }}">back to list</a>

    <a href="{{ path('tipo_pieza_edit', {'id': tipo_pieza.id}) }}">edit</a>

    {{ include('tipo_pieza/_delete_form.html.twig') }}
{% endblock %}
